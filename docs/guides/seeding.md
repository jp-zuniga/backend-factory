---
icon: lucide/tags
---

# Seeding

!!! note "Outline"

    Getting the rows an empty database needs, and the ones a demo needs.

- Seeders that run after migrations, and the lock that keeps them single-run.
- What belongs in a seeder: groups, permissions, catalogues.
- `populate` for local sample data, and why it never runs in production.
- Writing a management command, and where to put it.
